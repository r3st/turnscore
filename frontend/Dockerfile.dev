FROM node:24-alpine

WORKDIR /app

# Install dependencies first (layer caching)
COPY package*.json ./
RUN npm ci

# Source is mounted as volume in dev
EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
